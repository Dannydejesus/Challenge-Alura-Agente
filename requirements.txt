# ============================================================
# Challenge Alura Agente — Requirements
# Agente RAG de Recursos Humanos con Google Gemini + LangChain
# ============================================================

# --- LLM & Embeddings ---
cohere>=5.0.0                    # SDK oficial Cohere
langchain-cohere>=0.3.0          # Integracion LangChain + Cohere
langchain>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0  # Chunking (paquete separado en LangChain >= 0.2)
langchain-community>=0.2.0
langchain-chroma>=0.1.0

# --- Base Vectorial ---
chromadb>=0.5.0
pysqlite3-binary>=0.5.2   # Requerido por Streamlit Cloud para ChromaDB

# --- Extracción Multi-Formato ---
PyMuPDF>=1.24.0           # PDF
python-docx>=1.1.0        # Word (.docx)
openpyxl>=3.1.0           # Excel (.xlsx)
python-pptx>=0.6.23       # PowerPoint (.pptx)
beautifulsoup4>=4.12.0    # HTML
lxml>=5.2.0               # HTML/XML parser
unstructured>=0.14.0      # Multi-formato unificado

# --- Procesamiento de datos ---
pandas>=2.2.0
numpy>=1.26.0

# --- Interfaz ---
streamlit>=1.35.0

# --- Utilidades ---
python-dotenv>=1.0.0      # Variables de entorno
pydantic>=2.7.0           # Validación de datos
tiktoken>=0.7.0           # Conteo de tokens
tenacity>=8.3.0           # Reintentos automáticos
loguru>=0.7.0             # Logging avanzado

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
